c:/Projects/org.stephe_leake.music_player_2/app/src/main/java/org/stephe_leake/music_player_2/MainActivity.kt
use same design as old app
    start search activity from menu

    give up on preferences
        set useful defaults in code
        try DataStore later; ask Gemini to add it to small project

android 14 = api 34

testing
    create new playlist
        works, does not check if song is present, nor try to download it
            need to change code to use MediaStore
                which means changing playlist to store Album_Artist, album, song instead of filename.
                need web server API version

    show logfile by tapping on notification fails "can't open file"
        add to small project

    "cancel" in notification doesn't.

    eliminate EXTRA_COMMAND; add more top level commands.

    menu

        quit - does nothing; need play service

        search - does nothing; need new search activity

        share - does nothing; need utils.retriever. which needs play service.

After download new playlist, check that all files are present; download any missing


change web server command "download" to "get_song_list"
    send from DownloadUtils.kt getNewSongsList

record "last_downloaded" in getNewSongs, not in get file.


Need "clear playlist" and "delete playlist"; instrumental got screwed up.
    or just edit manually, since they are visible.

text in buttons is too light!
    test on phone


 Resources
tablet: Android 14
phone:  Android 14

AndroidStudio doesn't understand git worktree (sigh)
    gradle sync in work_1 fails mysteriously (sigh)

https://svg2vector.com/
    create svg image with InkScape, convert to Android drawable resource file.

https://developer.android.com/reference/android/R.drawable.html
    names of predefined icons

https://stackoverflow.com/questions/57466675/how-to-use-preferencescreen-of-androidx

Used Apk Extractor to copy apk to Google Drive Backup folder.
